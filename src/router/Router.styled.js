import { styled } from '@theme';

export const Container = styled.div`
  display: flex;
  align-items: center;
  padding: 20px;
`;

export const Area = styled(Container)`
  flex-direction: column;
`;
